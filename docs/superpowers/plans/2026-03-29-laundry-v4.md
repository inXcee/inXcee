# Laundry v4 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Çamaşırhane modülüne UI polish, renk+desen seçici, kıyafet ikonları, not sticker, kanban DnD fix, makine sayacı, günlük istatistik, hızlı ekleme ve genişletilmiş ayarlar ekle.

**Architecture:** 2 faz — Faz 1 (Task 1-5) sadece frontend değişiklikler, DB migration gerektirmez. Faz 2 (Task 6-10) backend migration + yeni sorgular + ilgili UI. Her task bağımsız commit alır.

**Tech Stack:** React 18, @tanstack/react-query, @dnd-kit/core, SQLite (better-sqlite3), Express, Vitest

---

## Dosya Haritası

| Dosya | Faz | Ne değişir |
|---|---|---|
| `frontend/src/modules/laundry/LaundryHub.jsx` | 1+2 | maxWidth, fonts, DnD fix, QuickNotes sticker, Quick Add panel, washed_today KPI |
| `frontend/src/modules/laundry/components/NewItemModal.jsx` | 1 | Kıyafet ikonları, renk+desen seçici |
| `frontend/src/modules/laundry/components/MachineStrip.jsx` | 2 | total_runs gösterimi |
| `frontend/src/modules/laundry/LaundrySettings.jsx` | 2 | Kıyafetler + Hedefler sekmeleri |
| `backend/src/shared/db/index.js` | 2 | total_runs ALTER migration |
| `backend/src/modules/laundry/queries.js` | 2 | total_runs increment, washed_today sorgusu, updateMachineQuery whitelist |
| `backend/src/modules/laundry/laundry.test.js` | 2 | total_runs testi |

---

## FAZ 1 — UI Polish

---

### Task 1: Sayfa Büyütme

**Files:**
- Modify: `frontend/src/modules/laundry/LaundryHub.jsx`

- [ ] **Adım 1: maxWidth ve kanban boyutlarını güncelle**

`LaundryHub.jsx` içinde `return (` sonraki ilk `<div>` satırını bul:
```jsx
// Bul:
<div style={{ maxWidth: view === 'kanban' ? 1200 : 880, position: 'relative', zIndex: 1 }} className="fade-up">

// Değiştir:
<div style={{ maxWidth: view === 'kanban' ? 1600 : 1000, position: 'relative', zIndex: 1 }} className="fade-up">
```

- [ ] **Adım 2: Header başlık font büyüt**

```jsx
// Bul:
<h1 style={{ fontFamily: 'var(--display)', fontSize: 30, letterSpacing: 5, color: 'var(--text)', lineHeight: 1, marginBottom: 8 }}>

// Değiştir:
<h1 style={{ fontFamily: 'var(--display)', fontSize: 36, letterSpacing: 5, color: 'var(--text)', lineHeight: 1, marginBottom: 8 }}>
```

- [ ] **Adım 3: Section tab font büyüt**

```jsx
// Bul (map içinde):
fontFamily: 'var(--mono)', fontSize: 9, letterSpacing: 1,

// Değiştir:
fontFamily: 'var(--mono)', fontSize: 11, letterSpacing: 1,
```

- [ ] **Adım 4: KPI kart büyük rakam font büyüt**

```jsx
// Bul:
<div style={{ fontFamily: 'var(--display)', fontSize: 44, letterSpacing: 2, color: s.color, lineHeight: 1, marginBottom: 6 }}>

// Değiştir:
<div style={{ fontFamily: 'var(--display)', fontSize: 52, letterSpacing: 2, color: s.color, lineHeight: 1, marginBottom: 6 }}>
```

- [ ] **Adım 5: KPI label font büyüt**

```jsx
// Bul:
<div style={{ fontFamily: 'var(--mono)', fontSize: 9, color: 'var(--text3)', textTransform: 'uppercase', letterSpacing: 2 }}>

// Değiştir (KPI kartının label'ı):
<div style={{ fontFamily: 'var(--mono)', fontSize: 10, color: 'var(--text3)', textTransform: 'uppercase', letterSpacing: 2 }}>
```

- [ ] **Adım 6: KanbanCol minWidth ve maxHeight büyüt**

```jsx
// Bul (KanbanCol fonksiyonunda):
flex: 1, minWidth: 220, display: 'flex', flexDirection: 'column',

// Değiştir:
flex: 1, minWidth: 260, display: 'flex', flexDirection: 'column',
```

```jsx
// Bul (KanbanCol içinde scrollable div):
flex: 1, overflowY: 'auto', padding: 8, display: 'flex', flexDirection: 'column', gap: 6, maxHeight: 520,

// Değiştir:
flex: 1, overflowY: 'auto', padding: 10, display: 'flex', flexDirection: 'column', gap: 8, maxHeight: 680,
```

- [ ] **Adım 7: Dev sunucu açık değilse başlat ve görsel doğrula**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude" && npm run dev
```

Tarayıcıda http://localhost:5174 → Çamaşırhane sayfasını aç, sayfa geniş ve büyük görünmeli.

- [ ] **Adım 8: Commit**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude"
git add frontend/src/modules/laundry/LaundryHub.jsx
git commit -m "feat: laundry v4 — sayfa büyütme, geniş layout"
```

---

### Task 2: Kanban DnD Fix

**Files:**
- Modify: `frontend/src/modules/laundry/LaundryHub.jsx`

- [ ] **Adım 1: Sensor activationConstraint güncelle**

```jsx
// Bul:
const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }))

// Değiştir:
const sensors = useSensors(useSensor(PointerSensor, {
  activationConstraint: { distance: 5, delay: 100, tolerance: 5 },
}))
```

- [ ] **Adım 2: DraggableKanbanCard transform düzelt**

```jsx
// Bul (DraggableKanbanCard return içinde):
<div
  ref={setNodeRef}
  style={{
    transform: CSS.Translate.toString(transform),
    opacity: isDragging ? 0.4 : 1,
    cursor: isDragging ? 'grabbing' : 'grab',
  }}
  {...attributes}
  {...listeners}
>

// Değiştir:
<div
  ref={setNodeRef}
  style={{
    transform: CSS.Transform.toString(transform),
    opacity: isDragging ? 0.3 : 1,
    cursor: isDragging ? 'grabbing' : 'grab',
    transition: isDragging ? 'none' : 'transform 0.2s ease, opacity 0.2s ease',
    zIndex: isDragging ? 999 : 'auto',
    position: 'relative',
    boxShadow: isDragging ? '0 12px 40px rgba(0,0,0,0.5)' : 'none',
    scale: isDragging ? '1.03' : '1',
  }}
  {...attributes}
  {...listeners}
>
```

- [ ] **Adım 3: Kanban kolonları arasında DragOverlay doğrulama**

`DragOverlay` zaten mevcut — `activeItem` null değilse `KanbanCard` render ediyor. Bunu kontrol et, varsa bırak.

- [ ] **Adım 4: Görsel doğrula**

Dev sunucu açık → kanban görünümüne geç → bir kartı sürükle. Kart scale ile büyümeli, bırakınca doğru kolona oturmalı, scroll tetiklememeli.

- [ ] **Adım 5: Commit**

```bash
git add frontend/src/modules/laundry/LaundryHub.jsx
git commit -m "fix: kanban DnD — activation constraint, transform, drag feedback"
```

---

### Task 3: Not Sticker (Sabit Köşe)

**Files:**
- Modify: `frontend/src/modules/laundry/LaundryHub.jsx`

- [ ] **Adım 1: QuickNotes bileşenini tamamen yeniden yaz**

`LaundryHub.jsx` içinde `function QuickNotes()` başlangıcından kapanışına kadar olan bloğu sil ve şununla değiştir:

```jsx
// ── QuickNotes (Sabit Sticker) ─────────────────────────────────
function QuickNotes({ visible }) {
  const [open, setOpen] = useState(false)
  const [notes, setNotes] = useState(() => {
    try { return localStorage.getItem('laundry-notes') || '' } catch { return '' }
  })

  const save = (val) => {
    setNotes(val)
    try { localStorage.setItem('laundry-notes', val) } catch {}
  }

  const lineCount = notes.split('\n').filter(l => l.trim()).length

  if (!visible) return null

  return (
    <div style={{ position: 'fixed', bottom: 24, right: 24, zIndex: 980 }}>
      {open ? (
        <div style={{
          width: 220, background: 'linear-gradient(135deg,#f5e642,#f0c030)',
          borderRadius: '4px 12px 12px 4px',
          borderLeft: '4px solid #c8a020',
          boxShadow: '4px 6px 20px rgba(0,0,0,0.5)',
          transform: 'rotate(-1.5deg)',
          overflow: 'hidden',
        }}>
          <div style={{
            padding: '8px 10px 4px',
            display: 'flex', justifyContent: 'space-between', alignItems: 'center',
          }}>
            <span style={{ fontFamily: 'var(--mono)', fontSize: 9, fontWeight: 700, color: '#2a1a00', letterSpacing: 1 }}>
              📋 NOTLAR
            </span>
            <button onClick={() => setOpen(false)} style={{
              background: 'none', border: 'none', cursor: 'pointer',
              fontSize: 12, color: '#2a1a00', padding: '0 2px',
            }}>✕</button>
          </div>
          <textarea
            value={notes}
            onChange={e => save(e.target.value)}
            placeholder="Kayıplar, özel talepler, acil notlar..."
            autoFocus
            style={{
              width: '100%', minHeight: 160, padding: '6px 10px 10px',
              background: 'transparent', border: 'none', resize: 'none',
              fontFamily: 'var(--mono)', fontSize: 11, color: '#1a1000',
              lineHeight: 1.7, outline: 'none', boxSizing: 'border-box',
            }}
          />
          {notes.trim() && (
            <div style={{ padding: '4px 10px 8px', display: 'flex', justifyContent: 'flex-end' }}>
              <button onClick={() => save('')} style={{
                background: 'rgba(0,0,0,0.1)', border: 'none', cursor: 'pointer',
                fontFamily: 'var(--mono)', fontSize: 8, color: '#2a1a00',
                padding: '2px 8px', borderRadius: 4,
              }}>Temizle</button>
            </div>
          )}
        </div>
      ) : (
        <button
          onClick={() => setOpen(true)}
          style={{
            width: 44, height: 44, borderRadius: '50%',
            background: 'linear-gradient(135deg,#f5e642,#f0c030)',
            border: '2px solid #c8a020',
            boxShadow: '2px 4px 12px rgba(0,0,0,0.4)',
            cursor: 'pointer', fontSize: 18,
            display: 'flex', alignItems: 'center', justifyContent: 'center',
            position: 'relative',
          }}
          title="Notlar"
        >
          📋
          {lineCount > 0 && (
            <span style={{
              position: 'absolute', top: -4, right: -4,
              width: 18, height: 18, borderRadius: '50%',
              background: 'var(--red)', color: '#fff',
              fontFamily: 'var(--mono)', fontSize: 9, fontWeight: 700,
              display: 'flex', alignItems: 'center', justifyContent: 'center',
            }}>{lineCount}</span>
          )}
        </button>
      )}
    </div>
  )
}
```

- [ ] **Adım 2: QuickNotes render çağrısını güncelle**

`LaundryHub` return içinde `section === 'hub'` bloğunu bul. Mevcut `<QuickNotes />` kullanımını kaldır, en sona ekle (modals'ın yanına):

```jsx
{/* Not Sticker — sadece hub'da */}
<QuickNotes visible={section === 'hub'} />
```

Ayrıca header bölümündeki eski `<QuickNotes />` buton çağrısını (varsa) temizle.

- [ ] **Adım 3: Görsel doğrula**

Tarayıcıda → Çamaşırhane hub → Sağ altta sarı yuvarlak buton görünmeli. Tıklayınca sarı not paneli açılmalı, not yazınca badge sayısı artmalı.

- [ ] **Adım 4: Commit**

```bash
git add frontend/src/modules/laundry/LaundryHub.jsx
git commit -m "feat: laundry — not sticker sabit köşe, post-it tasarımı"
```

---

### Task 4: Kıyafet İkonları

**Files:**
- Modify: `frontend/src/modules/laundry/components/NewItemModal.jsx`
- Modify: `frontend/src/modules/laundry/LaundryHub.jsx` (ExpandedSection)

- [ ] **Adım 1: CLOTHING_ICONS map'i NewItemModal.jsx'e ekle**

`NewItemModal.jsx` dosyasında `CLOTHING_TYPES` array'inin hemen altına ekle:

```js
export const CLOTHING_ICONS = {
  'Pantolon':      '👖',
  'Gömlek':        '👔',
  'T-Shirt':       '👕',
  'Kazak':         '🧥',
  'Sweat':         '👕',
  'Polar':         '🧥',
  'Mont':          '🧥',
  'Hırka':         '🧶',
  'Body':          '🩲',
  'İçlik':         '🩳',
  'Alt Eşofman':   '🩲',
  'Üst Eşofman':   '👕',
  'Boxer':         '🩲',
  'Külot':         '🩲',
  'Çorap':         '🧦',
  'Havlu Tkm':     '🏖️',
  'El Havlusu':    '🧻',
  'Ayak Havlusu':  '🧻',
  'Büyük Havlu':   '🛁',
  'Ceket':         '🥼',
  'Yastık K.':     '🛏️',
  'İş Mont':       '🦺',
  'İş Pantalonu':  '👖',
  'Şort':          '🩳',
  'Atlet':         '👕',
  'Diğer':         '📦',
}
```

- [ ] **Adım 2: Chip butonlarına ikon ekle**

```jsx
// Bul (chip butonları map içinde):
{active && '✓ '}{type}

// Değiştir:
{active && '✓ '}{CLOTHING_ICONS[type] || ''} {type}
```

- [ ] **Adım 3: Kıyafet satırında tip adına ikon ekle**

```jsx
// Bul (clothing.map satır içinde):
<span style={{ fontFamily: 'var(--mono)', fontSize: 11, color: 'var(--text)', flex: '0 0 70px' }}>{c.type}</span>

// Değiştir:
<span style={{ fontFamily: 'var(--mono)', fontSize: 11, color: 'var(--text)', flex: '0 0 90px' }}>
  {CLOTHING_ICONS[c.type] || ''} {c.type}
</span>
```

- [ ] **Adım 4: CLOTHING_ICONS'ı LaundryHub.jsx'e import et**

`LaundryHub.jsx` dosyasının import satırlarına ekle:

```js
import { CLOTHING_ICONS } from './components/NewItemModal.jsx'
```

- [ ] **Adım 5: ExpandedSection kıyafet preview'ine ikon ekle**

```jsx
// Bul (ExpandedSection içinde cl.map):
{c.qty}× {c.type}{c.color ? ` (${c.color})` : ''}

// Değiştir:
{CLOTHING_ICONS[c.type] || ''} {c.qty}× {c.type}{c.color ? ` (${c.color})` : ''}
```

- [ ] **Adım 6: KanbanCard preview satırına da ikon ekle**

```jsx
// Bul (KanbanCard içinde cl.slice(0,2).map):
const preview = cl.slice(0, 2).map(c => `${c.qty} ${c.type}`).join(' · ')

// Değiştir:
const preview = cl.slice(0, 2).map(c => `${CLOTHING_ICONS[c.type] || ''}${c.qty} ${c.type}`).join(' · ')
```

- [ ] **Adım 7: Görsel doğrula**

Yeni kayıt modal → kıyafet chip'lerinde ikonlar görünmeli. Var olan kayıtların expand bölümünde de ikonlar görünmeli.

- [ ] **Adım 8: Commit**

```bash
git add frontend/src/modules/laundry/components/NewItemModal.jsx
git add frontend/src/modules/laundry/LaundryHub.jsx
git commit -m "feat: laundry — kıyafet emoji ikonları chip ve kart görünümünde"
```

---

### Task 5: Renk + Desen Seçici

**Files:**
- Modify: `frontend/src/modules/laundry/components/NewItemModal.jsx`

- [ ] **Adım 1: PATTERN_LIST sabitini ekle**

`NewItemModal.jsx`'te `COLOR_PALETTE` tanımının hemen altına:

```js
const PATTERN_LIST = [
  { name: 'B/L Çizgili',     bg: 'repeating-linear-gradient(90deg,#f0f0f0 0 4px,#1a2e5e 4px 8px)' },
  { name: 'B/K Çizgili',     bg: 'repeating-linear-gradient(90deg,#f0f0f0 0 4px,#dc2626 4px 8px)' },
  { name: 'B/Y Çizgili',     bg: 'repeating-linear-gradient(90deg,#f0f0f0 0 4px,#eab308 4px 8px)' },
  { name: 'S/B Çizgili',     bg: 'repeating-linear-gradient(90deg,#888 0 4px,#f0f0f0 4px 8px)' },
  { name: 'Çapraz Çizgili',  bg: 'repeating-linear-gradient(45deg,#f0f0f0 0 4px,#1a2e5e 4px 8px)' },
  { name: 'Gri Kareli',      bg: 'repeating-conic-gradient(#888 0% 25%,#f0f0f0 0% 50%) 0 0/8px 8px' },
  { name: 'L/B Kareli',      bg: 'repeating-conic-gradient(#1a2e5e 0% 25%,#f0f0f0 0% 50%) 0 0/8px 8px' },
  { name: 'Renkli Karnaval', bg: 'repeating-linear-gradient(90deg,#e74c3c 0 6px,#f0a500 6px 12px,#2563eb 12px 18px,#16a34a 18px 24px)' },
  { name: 'L/B İki Renk',    bg: 'linear-gradient(135deg,#1a2e5e 50%,#f0f0f0 50%)' },
  { name: 'K/B İki Renk',    bg: 'linear-gradient(135deg,#dc2626 50%,#f0f0f0 50%)' },
]
```

- [ ] **Adım 2: Kıyafet satırına desen satırı ekle**

`NewItemModal.jsx`'teki `clothing.map` içinde renk paleti div'inin hemen altına (input'tan önce) desen satırını ekle:

```jsx
{/* Desen seçici */}
<div style={{ display: 'flex', flexWrap: 'wrap', gap: 4, alignItems: 'center', flex: 1, marginTop: 4 }}>
  <span style={{ fontFamily: 'var(--mono)', fontSize: 8, color: 'var(--text3)', flexShrink: 0 }}>DESEN:</span>
  {PATTERN_LIST.map(pat => (
    <button
      key={pat.name}
      title={pat.name}
      onClick={() => updateClothing(idx, 'color', c.color === pat.name ? '' : pat.name)}
      style={{
        width: 24, height: 24, borderRadius: 4,
        border: `2px solid ${c.color === pat.name ? 'var(--accent)' : 'transparent'}`,
        background: pat.bg, cursor: 'pointer', padding: 0, flexShrink: 0,
        boxShadow: c.color === pat.name ? '0 0 0 1px var(--accent)' : 'none',
        transition: 'border 0.1s',
        outline: 'none',
      }}
    />
  ))}
</div>
```

Renk paleti div'ini bul (`display: 'flex', flexWrap: 'wrap', gap: 4, alignItems: 'center', flex: 1`). Renk paletini ve desen satırını bir wrapper `<div>` içine al:

```jsx
<div style={{ flex: 1, display: 'flex', flexDirection: 'column', gap: 6 }}>
  {/* Renk paleti */}
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: 4, alignItems: 'center' }}>
    {COLOR_PALETTE.map(col => (
      // mevcut renk buton kodu - değişmez
    ))}
    <input ... />  {/* mevcut "Diğer" input */}
    {c.color && <span ...>{c.color}</span>}
  </div>
  {/* Desen satırı */}
  <div style={{ display: 'flex', flexWrap: 'wrap', gap: 4, alignItems: 'center' }}>
    <span style={{ fontFamily: 'var(--mono)', fontSize: 8, color: 'var(--text3)', flexShrink: 0 }}>DESEN:</span>
    {PATTERN_LIST.map(pat => (
      <button
        key={pat.name}
        title={pat.name}
        onClick={() => updateClothing(idx, 'color', c.color === pat.name ? '' : pat.name)}
        style={{
          width: 24, height: 24, borderRadius: 4,
          border: `2px solid ${c.color === pat.name ? 'var(--accent)' : 'transparent'}`,
          background: pat.bg, cursor: 'pointer', padding: 0, flexShrink: 0,
          boxShadow: c.color === pat.name ? '0 0 0 1px var(--accent)' : 'none',
          transition: 'border 0.1s',
          outline: 'none',
        }}
      />
    ))}
  </div>
</div>
```

- [ ] **Adım 3: Desen seçilince renk preview güncelle**

`c.color` artık desen adı olabilir. Renk preview span'ını güncelle — hem solid renk hem desen adını göstersin:

```jsx
// Mevcut color span'ını bul:
{c.color && (
  <span style={{ fontFamily: 'var(--mono)', fontSize: 9, color: 'var(--text3)', flexShrink: 0 }}>{c.color}</span>
)}
```

Bu zaten pattern adını da göstereceğinden değişiklik gerekmez.

- [ ] **Adım 4: Görsel doğrula**

Yeni kayıt modal → bir kıyafet seç → renk paleti + desen sırasına bakmalı. Desen seçince 24×24 kareler highlight olmalı, renk alanına desen adı yazılmalı.

- [ ] **Adım 5: Commit**

```bash
git add frontend/src/modules/laundry/components/NewItemModal.jsx
git commit -m "feat: laundry — renk+desen seçici, 10 ön tanımlı desen"
```

---

## FAZ 2 — Yeni Özellikler (DB Migration)

---

### Task 6: Backend — Makine total_runs

**Files:**
- Modify: `backend/src/shared/db/index.js`
- Modify: `backend/src/modules/laundry/queries.js`
- Modify: `backend/src/modules/laundry/laundry.test.js`

- [ ] **Adım 1: Test yaz (önce başarısız olacak)**

`laundry.test.js` dosyasının `describe` bloğuna ekle:

```js
it('makine total_runs timer başlatınca artar', async () => {
  const { listMachinesQuery, updateMachineQuery, getMachineQuery } = await import('./queries.js')
  const machines = listMachinesQuery()
  const m = machines[0]
  const before = m.total_runs ?? 0
  const timerEnd = new Date(Date.now() + 60 * 60000).toISOString()
  updateMachineQuery(m.id, { status: 'running', timer_end: timerEnd, increment_runs: true })
  const after = getMachineQuery(m.id)
  expect(after.total_runs).toBe(before + 1)
})
```

- [ ] **Adım 2: Test'i çalıştır — başarısız olduğunu doğrula**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude/backend"
npx vitest run src/modules/laundry/laundry.test.js
```

`total_runs` kolonu henüz yok — test fail etmeli.

- [ ] **Adım 3: DB migration ekle**

`backend/src/shared/db/index.js` dosyasında `timer_started_at` ALTER satırının hemen altına:

```js
try { db.exec(`ALTER TABLE laundry_machines ADD COLUMN total_runs INTEGER DEFAULT 0`) } catch(_) {}
```

- [ ] **Adım 4: updateMachineQuery'yi güncelle**

`queries.js` dosyasında `updateMachineQuery` fonksiyonunu bul ve değiştir:

```js
export function updateMachineQuery(id, fields) {
  const db = getDB()
  const { increment_runs, ...rest } = fields
  const allowed = ['name', 'type', 'status', 'timer_end', 'timer_started_at', 'capacity_kg', 'maintenance_notes', 'total_runs']
  const entries = Object.entries(rest).filter(([k]) => allowed.includes(k))
  if (!entries.length && !increment_runs) return
  const sets = entries.map(([k]) => `${k} = ?`)
  const vals = entries.map(([, v]) => v)
  if (increment_runs) {
    sets.push('total_runs = total_runs + 1')
  }
  if (!sets.length) return
  db.prepare(`UPDATE laundry_machines SET ${sets.join(', ')} WHERE id = ?`).run(...vals, id)
}
```

- [ ] **Adım 5: Testi tekrar çalıştır — geçtiğini doğrula**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude/backend"
npx vitest run src/modules/laundry/laundry.test.js
```

Tüm testler PASS olmalı.

- [ ] **Adım 6: Service katmanını güncelle**

`backend/src/modules/laundry/service.js` dosyasında `setTimer` veya makine timer başlatan fonksiyonu bul. `updateMachineQuery` çağrısını `increment_runs: true` ekleyerek güncelle:

```js
// Bul: setTimer veya updateMachine içinde timer_end set eden satır
// Örnek mevcut kod:
updateMachineQuery(id, { status: 'running', timer_end: timerEnd, timer_started_at: now })

// Değiştir:
updateMachineQuery(id, { status: 'running', timer_end: timerEnd, timer_started_at: now, increment_runs: true })
```

- [ ] **Adım 7: Tüm testleri çalıştır**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude/backend"
npx vitest run
```

Tüm testler PASS olmalı.

- [ ] **Adım 8: Commit**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude"
git add backend/src/shared/db/index.js backend/src/modules/laundry/queries.js backend/src/modules/laundry/laundry.test.js backend/src/modules/laundry/service.js
git commit -m "feat: laundry — makine total_runs sayacı, DB migration, test"
```

---

### Task 7: MachineStrip — total_runs Gösterimi

**Files:**
- Modify: `frontend/src/modules/laundry/components/MachineStrip.jsx`

- [ ] **Adım 1: MachineCard footer'ını güncelle**

`MachineStrip.jsx` içinde `MachineCard` bileşeninin footer div'ini bul:

```jsx
// Bul:
{m.active_items > 0 && (
  <div style={{
    marginTop: 4, paddingTop: 6, borderTop: '1px solid var(--border)',
    fontFamily: 'var(--mono)', fontSize: 9, color: 'var(--text3)',
    textAlign: 'center', letterSpacing: 0.5,
  }}>
    {m.active_items} yıkama
  </div>
)}

// Değiştir:
<div style={{
  marginTop: 4, paddingTop: 6, borderTop: '1px solid var(--border)',
  fontFamily: 'var(--mono)', fontSize: 9, color: 'var(--text3)',
  textAlign: 'center', letterSpacing: 0.5,
  display: 'flex', justifyContent: 'center', gap: 8,
}}>
  {m.active_items > 0 && <span>{m.active_items} yüklü</span>}
  {(m.total_runs > 0) && (
    <span style={{ color: 'var(--text4)' }}>{m.total_runs}× çalıştı</span>
  )}
  {!m.active_items && !m.total_runs && null}
</div>
```

- [ ] **Adım 2: Footer'ı her zaman göster (active_items 0 olsa bile total_runs varsa)**

Yukarıdaki kod zaten her iki durumu kapsıyor. `{m.active_items > 0 && ...}` yerine her zaman render edilecek şekilde güncellendi.

- [ ] **Adım 3: Görsel doğrula**

Backend çalışıyorken → Makine karta timer başlat → Sıfırla → total_runs footer'da `1× çalıştı` görünmeli.

- [ ] **Adım 4: Commit**

```bash
git add frontend/src/modules/laundry/components/MachineStrip.jsx
git commit -m "feat: laundry — makine kartında total_runs gösterimi"
```

---

### Task 8: Backend — washed_today + KPI Güncellemesi

**Files:**
- Modify: `backend/src/modules/laundry/queries.js`
- Modify: `frontend/src/modules/laundry/LaundryHub.jsx`

- [ ] **Adım 1: getStatsQuery'ye washed_today ekle**

`queries.js`'te `getStatsQuery` fonksiyonu içinde `delivered_today` sorgusunun hemen altına:

```js
const washed_today = db.prepare(`
  SELECT COUNT(*) as count FROM laundry_history
  WHERE to_status = 'washing'
    AND date(created_at) = date('now')
`).get()
```

- [ ] **Adım 2: Return'e ekle**

```js
// Bul:
return { by_status, delivered_today, avg_hours, sla_violations, period_total, period_delivered, machine_stats, by_room, lost_period, avg_delivery_hours, clothing_breakdown }

// Değiştir:
return { by_status, delivered_today, washed_today, avg_hours, sla_violations, period_total, period_delivered, machine_stats, by_room, lost_period, avg_delivery_hours, clothing_breakdown }
```

- [ ] **Adım 3: KPI strip'e 6. kart ekle**

`LaundryHub.jsx` içinde KPI strip array'ini bul (5 eleman olan `[...]`). Sonuna ekle:

```jsx
{ label: 'Bugün Yıkanan', value: stats?.washed_today?.count ?? 0, color: 'var(--blue)', sub: null },
```

`gridTemplateColumns: 'repeat(5, 1fr)'` satırını bul ve güncelle:

```jsx
// Bul:
display: 'grid', gridTemplateColumns: 'repeat(5, 1fr)', gap: 8, marginBottom: 16

// Değiştir:
display: 'grid', gridTemplateColumns: 'repeat(6, 1fr)', gap: 8, marginBottom: 16
```

- [ ] **Adım 4: Tüm testleri çalıştır**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude/backend"
npx vitest run
```

PASS olmalı.

- [ ] **Adım 5: Görsel doğrula**

Tarayıcıda → Hub → KPI strip'te "Bugün Yıkanan" 6. kart görünmeli.

- [ ] **Adım 6: Commit**

```bash
git add backend/src/modules/laundry/queries.js frontend/src/modules/laundry/LaundryHub.jsx
git commit -m "feat: laundry — washed_today istatistik, KPI 6. kart"
```

---

### Task 9: Hızlı Kıyafet Ekleme (Quick Add)

**Files:**
- Modify: `frontend/src/modules/laundry/LaundryHub.jsx`

- [ ] **Adım 1: QuickAdd bileşenini yaz**

`LaundryHub.jsx`'te `FullRecordsView` fonksiyonunun hemen üstüne ekle:

```jsx
// ── QuickAdd ───────────────────────────────────────────────────
function QuickAdd({ onClose }) {
  const qc = useQueryClient()
  const [name, setName] = useState('')
  const [selected, setSelected] = useState([])
  const QUICK_TYPES = ['Pantolon','Gömlek','T-Shirt','Çorap','Boxer','Havlu Tkm','Kazak','Şort']

  const toggle = (type) => setSelected(prev =>
    prev.includes(type) ? prev.filter(t => t !== type) : [...prev, type]
  )

  const create = useMutation({
    mutationFn: () => laundryApi.createItem({
      room_id: null,
      intake_name: name.trim(),
      item_count: selected.length || 1,
      clothing_items: selected.length > 0
        ? selected.map(t => ({ type: t, color: '', qty: 1 }))
        : undefined,
      urgent: 0,
    }),
    onSuccess: () => { qc.invalidateQueries({ queryKey: ['laundry-items'] }); onClose() },
  })

  return (
    <div style={{
      background: 'var(--surface)', border: '1px solid var(--border)',
      borderTop: '2px solid var(--accent)', borderRadius: 10,
      padding: '14px 16px', marginBottom: 14,
    }}>
      <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 10, flexWrap: 'wrap' }}>
        <input
          className="form-input"
          value={name}
          onChange={e => setName(e.target.value)}
          placeholder="Ad Soyad (zorunlu)..."
          style={{ flex: '1 1 180px', minWidth: 140 }}
          autoFocus
        />
        <div style={{ display: 'flex', flexWrap: 'wrap', gap: 5 }}>
          {QUICK_TYPES.map(type => (
            <button key={type} onClick={() => toggle(type)} style={{
              padding: '4px 10px', borderRadius: 16, cursor: 'pointer',
              background: selected.includes(type) ? 'rgba(240,165,0,0.15)' : 'var(--surface2)',
              border: `1px solid ${selected.includes(type) ? 'rgba(240,165,0,0.4)' : 'var(--border)'}`,
              color: selected.includes(type) ? 'var(--accent)' : 'var(--text2)',
              fontFamily: 'var(--mono)', fontSize: 10,
            }}>
              {CLOTHING_ICONS[type] || ''} {type}
            </button>
          ))}
        </div>
        <button
          className="btn btn-primary"
          onClick={() => create.mutate()}
          disabled={!name.trim() || create.isPending}
          style={{ whiteSpace: 'nowrap' }}
        >
          {create.isPending ? '...' : `+ Kaydet${selected.length > 0 ? ` (${selected.length})` : ''}`}
        </button>
        <button className="btn btn-ghost btn-xs" onClick={onClose}>İptal</button>
      </div>
      {create.isError && (
        <div className="alert alert-danger" style={{ marginTop: 6, fontSize: 10 }}>
          {create.error?.response?.data?.error || 'Hata oluştu'}
        </div>
      )}
    </div>
  )
}
```

- [ ] **Adım 2: QuickAdd state ve butonu LaundryHub'a ekle**

`LaundryHub` fonksiyonunda state'lerin arasına:

```js
const [showQuickAdd, setShowQuickAdd] = useState(false)
```

Header `+ Yeni Kayıt` butonunun yanına:

```jsx
{section === 'hub' && (
  <>
    <button className="btn btn-ghost btn-xs"
      onClick={() => setShowQuickAdd(s => !s)}
      style={{
        border: `1px solid ${showQuickAdd ? 'rgba(240,165,0,0.3)' : 'var(--border)'}`,
        color: showQuickAdd ? 'var(--accent)' : 'var(--text3)',
      }}
    >
      ⚡ Hızlı Ekle
    </button>
    <button className="btn btn-primary" onClick={() => setShowNew(true)} style={{ letterSpacing: 1 }}>
      + Yeni Kayıt
    </button>
  </>
)}
```

- [ ] **Adım 3: QuickAdd panelini render et**

`section === 'hub'` bloğunun içinde, `<SlaAlert />` satırından hemen önce:

```jsx
{showQuickAdd && <QuickAdd onClose={() => setShowQuickAdd(false)} />}
```

- [ ] **Adım 4: Backend — room_id null'a izin ver**

`backend/src/modules/laundry/queries.js` içinde `insertItemQuery` fonksiyonunu bul. `room_id` için `NOT NULL` kısıtı yoksa (şema nullable), değişiklik gerekmez. Kontrol et:

```bash
grep -A5 "insertItemQuery" "/c/Users/hrync/OneDrive/Masaüstü/test claude/backend/src/modules/laundry/queries.js"
```

Eğer `room_id NOT NULL` gibi bir doğrulama varsa kaldır.

- [ ] **Adım 5: Görsel doğrula**

Hub → "⚡ Hızlı Ekle" → panel açılmalı → isim yaz + kıyafet seç → Kaydet → kanban'da dirty kolonuna düşmeli.

- [ ] **Adım 6: Tüm testleri çalıştır**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude/backend"
npx vitest run
```

- [ ] **Adım 7: Commit**

```bash
git add frontend/src/modules/laundry/LaundryHub.jsx
git commit -m "feat: laundry — hızlı kıyafet ekleme (quick add) paneli"
```

---

### Task 10: Ayarlar Genişletme

**Files:**
- Modify: `frontend/src/modules/laundry/LaundrySettings.jsx`

- [ ] **Adım 1: LaundrySettings'e sekme state'i ekle**

`LaundrySettings` bileşeninin başına:

```jsx
const [tab, setTab] = useState('sla') // 'sla' | 'machines' | 'clothing' | 'goals'
```

- [ ] **Adım 2: Sekme nav'ı ekle**

Mevcut `return (` içindeki ilk panel header'ının hemen altına sekme butonları ekle:

```jsx
<div style={{ display: 'flex', gap: 4, marginBottom: 20, borderBottom: '1px solid var(--border)', paddingBottom: 10 }}>
  {[
    { key: 'sla',      label: '⏱ SLA' },
    { key: 'machines', label: '⚙ Makineler' },
    { key: 'clothing', label: '👕 Kıyafetler' },
    { key: 'goals',    label: '🎯 Hedefler' },
  ].map(t => (
    <button key={t.key} onClick={() => setTab(t.key)} style={{
      padding: '5px 12px', borderRadius: 6, cursor: 'pointer',
      fontFamily: 'var(--mono)', fontSize: 10, letterSpacing: 1,
      background: tab === t.key ? 'rgba(240,165,0,0.12)' : 'transparent',
      border: `1px solid ${tab === t.key ? 'rgba(240,165,0,0.3)' : 'var(--border)'}`,
      color: tab === t.key ? 'var(--accent)' : 'var(--text3)',
    }}>{t.label}</button>
  ))}
</div>
```

- [ ] **Adım 3: Mevcut içeriği SLA ve Machines sekmelerine al**

Mevcut SLA tablosu `{tab === 'sla' && ...}` ile wrap et.
Mevcut makine formu `{tab === 'machines' && ...}` ile wrap et.

- [ ] **Adım 4: Kıyafetler sekmesi ekle**

```jsx
{tab === 'clothing' && (
  <ClothingSettings />
)}
```

`ClothingSettings` bileşenini `LaundrySettings.jsx` içine yaz:

```jsx
function ClothingSettings() {
  const DEFAULT_TYPES = [
    'Pantolon','Gömlek','T-Shirt','Kazak','Sweat','Polar','Mont','Hırka',
    'Body','İçlik','Alt Eşofman','Üst Eşofman','Boxer','Külot','Çorap',
    'Havlu Tkm','El Havlusu','Ayak Havlusu','Büyük Havlu','Ceket',
    'Yastık K.','İş Mont','İş Pantalonu','Şort','Atlet','Diğer',
  ]

  const [types, setTypes] = useState(() => {
    try {
      const saved = localStorage.getItem('custom-clothing-types')
      return saved ? JSON.parse(saved) : DEFAULT_TYPES
    } catch { return DEFAULT_TYPES }
  })
  const [newType, setNewType] = useState('')

  const save = (list) => {
    setTypes(list)
    try { localStorage.setItem('custom-clothing-types', JSON.stringify(list)) } catch {}
  }

  const add = () => {
    const t = newType.trim()
    if (!t || types.includes(t)) return
    save([...types, t])
    setNewType('')
  }

  const remove = (type) => save(types.filter(t => t !== type))

  const reset = () => { save(DEFAULT_TYPES); }

  return (
    <div>
      <label className="form-label">KIYAFETLERİ ÖZELLEŞTIR</label>
      <div style={{ display: 'flex', flexWrap: 'wrap', gap: 6, marginBottom: 14 }}>
        {types.map(type => (
          <span key={type} style={{
            display: 'flex', alignItems: 'center', gap: 5,
            padding: '4px 10px', borderRadius: 16,
            background: 'var(--surface2)', border: '1px solid var(--border)',
            fontFamily: 'var(--mono)', fontSize: 10, color: 'var(--text)',
          }}>
            {type}
            <button onClick={() => remove(type)} style={{
              background: 'none', border: 'none', cursor: 'pointer',
              color: 'var(--text3)', fontSize: 11, padding: 0, lineHeight: 1,
            }}>✕</button>
          </span>
        ))}
      </div>
      <div style={{ display: 'flex', gap: 8, alignItems: 'center', marginBottom: 10 }}>
        <input
          className="form-input"
          value={newType}
          onChange={e => setNewType(e.target.value)}
          onKeyDown={e => e.key === 'Enter' && add()}
          placeholder="Yeni kıyafet tipi..."
          style={{ flex: 1 }}
        />
        <button className="btn btn-sm" style={{ background: 'var(--accent)', color: '#000' }}
          onClick={add} disabled={!newType.trim()}>
          + Ekle
        </button>
      </div>
      <button className="btn btn-ghost btn-xs" onClick={reset} style={{ color: 'var(--text3)', fontSize: 9 }}>
        Varsayılana Sıfırla
      </button>
    </div>
  )
}
```

- [ ] **Adım 5: NewItemModal kıyafet listesini localStorage'dan oku**

`NewItemModal.jsx` içinde `CLOTHING_TYPES` sabitini dinamik hale getir:

```js
// Dosyanın başındaki CLOTHING_TYPES sabitini kaldır, yerine:
const DEFAULT_CLOTHING_TYPES = [
  'Pantolon','Gömlek','T-Shirt','Kazak','Sweat','Polar','Mont','Hırka',
  'Body','İçlik','Alt Eşofman','Üst Eşofman','Boxer','Külot','Çorap',
  'Havlu Tkm','El Havlusu','Ayak Havlusu','Büyük Havlu','Ceket',
  'Yastık K.','İş Mont','İş Pantalonu','Şort','Atlet','Diğer',
]
```

`NewItemModal` fonksiyon içinde:

```js
const CLOTHING_TYPES = (() => {
  try {
    const saved = localStorage.getItem('custom-clothing-types')
    return saved ? JSON.parse(saved) : DEFAULT_CLOTHING_TYPES
  } catch { return DEFAULT_CLOTHING_TYPES }
})()
```

- [ ] **Adım 6: Hedefler sekmesi ekle**

```jsx
{tab === 'goals' && (
  <GoalsSettings />
)}
```

`GoalsSettings` bileşeni:

```jsx
function GoalsSettings() {
  const [goal, setGoal] = useState(() => {
    try { return parseInt(localStorage.getItem('laundry-daily-goal') || '50') } catch { return 50 }
  })
  const [saved, setSaved] = useState(false)

  const save = () => {
    try { localStorage.setItem('laundry-daily-goal', String(goal)) } catch {}
    setSaved(true)
    setTimeout(() => setSaved(false), 2000)
  }

  return (
    <div>
      <label className="form-label">GÜNLÜK HEDEF</label>
      <div style={{ display: 'flex', gap: 10, alignItems: 'center', marginBottom: 16 }}>
        <input
          type="number"
          className="form-input"
          value={goal}
          min={1}
          max={9999}
          onChange={e => setGoal(Math.max(1, parseInt(e.target.value) || 1))}
          style={{ width: 100 }}
        />
        <span style={{ fontFamily: 'var(--mono)', fontSize: 10, color: 'var(--text3)' }}>parça/gün</span>
        <button className="btn btn-sm" style={{ background: 'var(--accent)', color: '#000' }} onClick={save}>
          {saved ? '✓ Kaydedildi' : 'Kaydet'}
        </button>
      </div>
      <div style={{ fontFamily: 'var(--mono)', fontSize: 9, color: 'var(--text3)', lineHeight: 1.6 }}>
        Bu hedef KPI strip'te "Bugün Yıkanan" kartında referans alınır.
        Hedefe ulaşıldığında kart rengi yeşile döner.
      </div>
    </div>
  )
}
```

- [ ] **Adım 7: KPI "Bugün Yıkanan" kartına hedef rengi uygula**

`LaundryHub.jsx`'te KPI array'inde "Bugün Yıkanan" kartını bul ve rengi dinamik yap:

```jsx
// Bul ve değiştir (array içinde Bugün Yıkanan kartı):
{
  label: 'Bugün Yıkanan',
  value: stats?.washed_today?.count ?? 0,
  color: (() => {
    try {
      const goal = parseInt(localStorage.getItem('laundry-daily-goal') || '50')
      const val = stats?.washed_today?.count ?? 0
      return val >= goal ? 'var(--green)' : 'var(--blue)'
    } catch { return 'var(--blue)' }
  })(),
  sub: null,
},
```

- [ ] **Adım 8: Görsel doğrula**

Ayarlar → 4 sekme görünmeli. Kıyafetler → yeni tip ekle → Yeni Kayıt modal aç → listede görünmeli. Hedefler → hedef kaydet → KPI rengi değişmeli.

- [ ] **Adım 9: Commit**

```bash
git add frontend/src/modules/laundry/LaundrySettings.jsx frontend/src/modules/laundry/components/NewItemModal.jsx frontend/src/modules/laundry/LaundryHub.jsx
git commit -m "feat: laundry — ayarlar genişletme, kıyafet yönetimi, günlük hedef"
```

---

## Son Adım: GitHub'a Push

- [ ] **Tüm testleri çalıştır**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude/backend"
npx vitest run
```

- [ ] **Push**

```bash
cd "/c/Users/hrync/OneDrive/Masaüstü/test claude"
git push origin main
```
